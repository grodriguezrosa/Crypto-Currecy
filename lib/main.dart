import 'package:flutter/material.dart';

void main() {
  runApp(const CryptoActivesApp());
}

class CryptoActivesApp extends StatelessWidget {
  const CryptoActivesApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}